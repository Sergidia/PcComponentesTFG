package ucm.tfg.pccomponentes.main

/**
 * Tipos de autenticación, este enum se usará cuando se añadan nuevos tipos de autenticación y haya que distinguirlos de cara al login/registro
 */
enum class ProviderType {
    BASIC //Autenticación con correo y contraseña
}